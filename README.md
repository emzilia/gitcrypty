# gitcrypty
```
Usage: gitcrypty [add/decrypt] (file)
        Encrypt/decrypt files before pushing to a git repository.

        add     Encrypts the file, then add its to the repo
        decrypt Decrypts all encrypted files within the folder
```
